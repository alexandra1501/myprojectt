%h2 All Requests

%table#tickets
  %thead
    %tr
      %th Name
      %th Subject
  %tbody
    - @tickets.each do |ticket|
      %tr
        %td= ticket.name
        %td= ticket.subject
        %td= link_to "Edit", edit_ticket_path(ticket)
        %td= link_to "Destroy", ticket_path(ticket), :method=>:delete
= link_to 'Add new ticket', new_ticket_path
